# Default session layout
# Main window (takes most of the space)
launch --title main
# Smaller pane on the right for quick commands
launch --location=vsplit --title side
# Bottom pane (horizontal split under main) for logs/servers
launch --location=hsplit --title bottom
# Focus back on main pane
focus_matching_window title:main
